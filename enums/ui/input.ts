export enum INPUT_TAGS {
  INPUT = 'input',
  TEXTAREA = 'textarea',
}

export enum INPUT_TYPES {
  TEXT = 'text',
  PASSWORD = 'password',
  EMAIL = 'email',
  NUMBER = 'number',
  TIME = 'time',
  DATE = 'date',
  WEEK = 'week',
  TEL = 'tel',
  URL = 'url',
  SEARCH = 'search',
}
